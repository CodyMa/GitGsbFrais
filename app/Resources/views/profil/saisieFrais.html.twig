{% extends 'profil/profil.html.twig' %}
{% block stylesheet %}
    {{ parent() }}
{% endblock stylesheet %}

{% block contenu %}
<div class="container">
    <div class ='row'>
        <div class="col-sm">
            <div class="container">
                <h1>Saisie de la fiche des frais</h1><br>
                <div class="container-fluid">
                    {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_row(form.login) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.mdp) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.choices) }}
                    </div>
                    <br>
                    <div id="divSave">
                        {{ form_row(form.save) }}
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock contenu%}

{% block affichage %}
{% endblock affichage %}